{
	"name": "functions",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"build": "esbuild functions/src/index.js --bundle --outfile=dist/index.js --target=es2020"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"esbuild": "^0.25.5",
		"vitest": "~3.2.0",
		"wrangler": "^4.23.0"
	},
	"dependencies": {
		"itty-router": "^5.0.18",
		"stripe": "^18.3.0"
	}
}
